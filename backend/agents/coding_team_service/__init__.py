"""Composition root for the standalone coding-team service (out of the coding_team package).

See ``coding_team_service.main`` — it installs the SE-backed engine provider and
exposes the coding_team app as the container's ``TEAM_MODULE``.
"""
