"""Coding team routers, mounted in-process by the software_engineering_team app.

``backend/agents`` is already importable by the time this package is reached
(the SE ``api`` package's own ``_paths`` bootstrap runs first), so no separate
sys.path bootstrap is needed here.
"""
